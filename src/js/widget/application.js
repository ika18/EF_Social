define(["compose",
    "jquery",
    'troopjs-core/widget/application'], function ApplicationModule(Compose, $, Application) {
    "use strict";

    // Specific Application start action here
    // Add according to project

    return Application.extend({
    });
});